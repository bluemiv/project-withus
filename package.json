{
  "name": "withus",
  "packageManager": "yarn@3.2.2",
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "web": "yarn workspace @withus/web",
    "server": "yarn workspace @withus/server",
    "docker:start": "docker-compose up -d",
    "docker:stop": "docker-compose down",
    "docker:logs": "docker-compose logs"
  },
  "devDependencies": {
    "@types/jest": "^27.5.2",
    "@types/node": "^16.11.47",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  },
  "engines": {
    "node": ">=16.15.1"
  }
}
